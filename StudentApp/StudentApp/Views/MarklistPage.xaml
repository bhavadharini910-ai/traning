<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="StudentApp.Views.MarklistPage">

    <CollectionView ItemsSource="{Binding Students}">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Frame Padding="10" Margin="5">
                    <VerticalStackLayout>

                        <Label Text="{Binding Name}" FontSize="18"/>

                        <Label Text="{Binding Mark1, StringFormat='Mark1: {0}'}"/>
                        <Label Text="{Binding Mark2, StringFormat='Mark2: {0}'}"/>
                        <Label Text="{Binding Mark3, StringFormat='Mark3: {0}'}"/>

                        <Label Text="{Binding Total, StringFormat='Total: {0}'}"/>

                    </VerticalStackLayout>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>

</ContentPage>